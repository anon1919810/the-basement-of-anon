# 《脉冲球》v0.0.4 执行清单（混合导演模式：Rapier 物理球）

> 依据：设计文档 v0.1（FC×FM 定位）、B 档讨论（引擎定结果 + 物理表现层）
> 状态：`[x]` = 已完成（含提交 hash）

## 物理球（Rapier 0.12，@dimforge/rapier3d-compat）

- [x] **异步初始化**：`MatchScene.create()` 静态工厂 await RAPIER.init()，MatchView 防竞态接入（切比赛安全）
- [x] **物理世界**：重力 -9.8 + 地面碰撞体 + 球动态刚体（ball collider，restitution 0.52 / friction 0.5 / 线性阻尼）
- [x] **弹簧-阻尼力驱动**：轨迹采样点为引力目标（水平 K_F=42 / 垂直 K_FY=30 / DAMP=4.2）→ 球有质量感/惯性/超调，而非瞬移
- [x] **踢球冲量**：跨事件时沿 起点→终点 给初速（空中事件按弧高算垂直初速），力驱动随后修正
- [x] **近点吸附**：距目标 <0.4m 或段末 → 强制落位防漂移（回放/步进/跳节同步不破）
- [x] **跳节直落**：所需速度超上限（大步进/跳节）→ 直接落位，干净跳转
- [x] **滚动自转**：球沿水平速度方向滚（角速度 = 线速度/半径）
- [x] **重力弧线 + 落地反弹**：空中事件真抛体下落、落地自然弹跳（物理引擎自带）

## 同步与确定性

- [x] 物理步进 = 真实帧 dt（1/60 固定步长，dt 上限 0.1）——变速/暂停/跳节不受影响
- [x] 引擎**零改动**：sim 11 断言全过，seed 42 帝国 16:11 洛林，结果与 v0.0.3 一致

## 验证与交付

- [x] `npm run build` 0 错误（rapier wasm 内联，产物 ~2.7MB）
- [x] Playwright 实测：`window.__pulse3dReady` true、canvas=1、无报错、回放界面正常（比分/脉冲/事件日志滚动）
- [x] 提交推送 + 清单勾选

## 下一步（v0.0.5 球员物理碰撞）

- [ ] 18 名球员加 kinematic 胶囊碰撞体 → 球被球员身体推开、卡位推挤
- [ ] 球员-球员碰撞（动态化，身体对抗）
- [ ] 物理参数实测定调（弹簧刚度/阻尼/反弹）
