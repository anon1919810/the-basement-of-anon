# 撷菁轩 · React + FastAPI 迁移计划（任务说明书）

> 本文件是项目的**唯一权威续命文档**。会话上下文清空后，新会话 / Cline 只要读取本仓库 + 本文件即可无缝继续。
> 版本基线：**v4.7.0 车书万里**（Streamlit 生产版，在线稳定）
> 作者署名：失败主义谋士千早爱音 ｜ 联系方式：QQ 169636694 / youxiang051110@163.com
> 致谢：王小波和纪德，所有提供建议和素材的朋友们。

---

## 0. 一句话目标

把「地方志文化要素提取」工具从 **Streamlit 单文件应用** 迁移为 **React 前端 + FastAPI 后端** 的现代架构，
**零停机**上线，托管成本 **¥0/月**（Render 免费档 + UptimeRobot 保温），保留全部既有功能与数据。

---

## 1. 项目现状（2025 年基线）

### 1.1 功能
- PDF / Word（docx）上传 → AI 提取文化要素，分 5 大类：
  物质文化 / 精神文化 / 制度文化 / 行为文化 / 心理文化，按流域（长江 / 汉江）分类。
- 多轮提取并集（默认 3 轮，自适应收敛），保障召回率（修复过"只出 1 条"的致命 bug）。
- 子目模式：识别【子目】块与「名称：正文」结构，仅提取**最大子目**（默认开启），避免过度提取。
- 历史文献保真：引文原样摘录（宁短勿改）、名称修正标注（原书作『』）、引文待核对标记（⚠）。
- OCR：RapidOCR（本地，能读【】）＞ Tesseract 兜底；OCR_DPI=300。
- 人工评分（1-5⭐）、AI 工作台（流式对话+条目补充）、留言板、统计看板（Plotly 中国地图）、在线编辑表格 + 下载 Excel。
- 用户体系：用户名+密码（salted sha256）、会话 24h、邀请码（931226）、每用户独立 API Key（加密存储）。

### 1.2 代码模块（Streamlit 版，全部在仓库根目录）
| 文件 | 职责 |
|---|---|
| `app.py` | Streamlit UI（侧边栏三页：提取工作台 / 管理后台 / 使用帮助），Supabase 风格（白底、直角边框、细 1px 线、绿色 #3ecf8e 强调色） |
| `extract_papers.py` | 编排：DeepSeek API 调用（`chat_completion` / `chat_completion_stream` SSE）、多轮提取、并行处理 PDF |
| `prompts.py` | 提示词构建（含"宁可多提取不遗漏"规则）、子目解析、目录/子目检测 |
| `ocr_engine.py` | RapidOCR + Tesseract、docx 提取、结构化标题识别、OCR 文本清洗 |
| `postprocess.py` | JSON 解析容错、名称/省份规范化（模糊索引）、合并去重、引文校验 |
| `ocr_corrections.py` | 形近字纠错表（潮→湖）、全局安全替换、已知实体库 |
| `province_dict.py` | 湖北省全量 + 12 省地名词典 |
| `database.py` | Supabase 数据层：会话令牌、API Key 加密、提取结果、评分、用户/管理函数 |
| `config.py` | 版本号、功能开关（STRICT_EXTRACTION、STRUCTURE_MODE、EXTRACT_MAX_ONLY、OCR_DPI、EXTRACTION_PASSES=3、PROMPT_TEMPLATE_VERSION=4.2） |
| `db_schema.sql` | users / messages / extraction_results 表 + RLS 策略（幂等创建） |
| `_run_regression.py` | 回归测试（4 用例：遗址 100%/100%、重庆 docx 86%/97%、江西 docx 86%/100%、扫描 100%/100%） |
| `_learn_corrections.py` / `_run_test.py` / `_run_ocr_batch.py` | 工具脚本 |

### 1.3 数据层（Supabase，云上已就绪）
- 项目 `vnnhcveudcoetzcuceun.supabase.co`（发布密钥 `sb_publishable_occHvZCJl5DmiNlL08qsdQ_BnQ6szlR`）
- 表：`users`（含 api_key / invite_until / is_admin）、`messages`、`extraction_results`（BIGSERIAL 主键）
- **认证在应用层**（HMAC 会话令牌），不用 Supabase Auth（auth.uid() 恒为空），RLS 策略全部放行。
- 生产现状：1 用户、10 次提取、519 条目、平均 9.0 条/次。

### 1.4 部署（现状）
- Streamlit Cloud 在线；云 Secrets：SESSION_SECRET / INVITE_CODE=931226 / ADMIN_USERNAMES=失败主义谋士千早爱音。
- 本地 `.env`（**不入库**）：DEEPSEEK_API_KEY、SUPABASE_URL、SUPABASE_KEY、SESSION_SECRET、INVITE_CODE、ADMIN_USERNAMES。
- 本地环境：Windows、Python 3.14.7（`.venv`）、PowerShell；**内联 `python -c` 引号易坏，一律写临时 .py 脚本**。

---

## 2. 目标架构（Route A：后端先行，零停机）

```
浏览器 (React SPA)
   │  HTTPS
   ▼
Render 免费档（FastAPI 后端）── 数据库 ──► Supabase（不动）
   │  SSE 进度流
   ▼
DeepSeek API（AI 提取 / 对话）
```

- **Phase 1**：FastAPI 后端（`backend/`），与 Streamlit 版**双跑**，验证通过后再切流量。
- **Phase 2**：React 前端（Vite + TypeScript + Tailwind + shadcn），部署到 **Vercel** 免费档。
- **Phase 3**：域名/入口切到新前端；Streamlit 版退役（保留分支 tag）。
- **保温**：UptimeRobot 每 10 分钟 ping 一次 `/api/health`，防 Render 免费档休眠。
- **成本**：Render 免费 + Vercel 免费 + UptimeRobot 免费 = **¥0/月**。
  远期：国内轻量服务器（¥100-200/年）或 Render 付费 $7/月（用户自行决定，不急）。

### 2.1 必须保留的行为（验收红线）
1. 提取结果与 v4.7.0 **逐条可比**：同一输入文件 + 同一配置 → 结果并集一致（缓存键相同）。
2. 多轮并集 + 自适应收敛（growth<25% 且 prev≥8 时收敛）。
3. 子目模式 / 最大子目逻辑、引文保真、名称修正、引文待核对标记。
4. 用户体系与数据完全复用 Supabase（不做数据迁移）。
5. 流式对话（SSE 打字机效果）、AI 补充条目按钮、评分、留言板、统计看板。
6. UI 视觉延续 Supabase 风格（白底 / 直角 / 1px 细线 / #3ecf8e）。
7. 回归测试 4 用例必须全绿。

---

## 3. 任务清单（Cline / 后续会话照此执行）

> 规则：**一次只做 1-2 个 ticket**；每完成一个 ticket 跑一次回归 + 提交一次 git；
> **任何 ticket 都不许动 `app.py` 等生产 Streamlit 文件**（除非 ticket 明确标注"修复生产"）。

### Phase 1：FastAPI 后端（`backend/`）

- [x] **T1 后端骨架**（已提交 `backend/`，冒烟测试通过）：FastAPI + uvicorn；`/api/health`；环境变量加载（`python-dotenv`，复用根 `.env`）；CORS 白名单；日志模块；`backend/smoke_test.py` 冒烟测试。
- [x] **T2 配置与数据层移植**（已提交）：`config.py`（调参常量）、`database.py`、`province_dict.py`、`ocr_corrections.py` 已进 `backend/app/`，行为一致；新增 `env.py` 加载根 `.env`；`/api/health` 接入真实 Supabase 连通自检；`backend/test_core.py` 单元测试 16 项全过（会话令牌/Key加解密/密码哈希/邀请码）。
- [ ] **T3 认证 API**：`POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me`；HMAC 会话令牌 24h（复用现有 `SESSION_SECRET` 算法，保证旧令牌逻辑等价）；邀请码校验；`GET /api/auth/key-status`（Key 来源解析：用户>管理员>邀请码>禁用，沿用 REQUIRE_KEY_OR_INVITE 语义）。
- [ ] **T4 提取核心移植**：把 `extract_papers.py` + `prompts.py` + `postprocess.py` + `ocr_engine.py` 移植为后端服务模块（**不改提取算法**，只改 IO 为 API 形态）；保留缓存键公式（md5(text|book|config 签名)）；保留多轮并集与低产重试。
- [ ] **T5 提取 API（同步）**：`POST /api/extract`（multipart 上传 → 同步处理 → 返回条目 JSON + 缓存命中标记）；保存 extraction_results；10MB 限制；每 run 独立临时目录 + 事后清理。
- [ ] **T6 提取 API（SSE 进度流）**：`POST /api/extract/stream`，SSE 推送阶段进度（解析/OCR/第 N 轮提取/后处理）；断连时任务可继续（后台执行）或取消；**前端工作台进度条依赖此接口**。
- [ ] **T7 对话与补充 API**：`POST /api/chat`（SSE 流式，复用 `chat_completion_stream`）；`POST /api/supplement`（基于现有条目做 AI 补充，非流式）；鉴权复用会话令牌。
- [ ] **T8 评分 / 留言板 API**：`POST /api/rating`、`GET /api/stats`（看板数据：总条目、按类/流域分布、高分条目）；留言板 CRUD（复用 messages 表）。
- [ ] **T9 管理后台 API**：用户列表 / 封禁解封 / 邀请码管理 / 全局 API Key 设置 / 提取记录删除（复用 database.py 的 admin 函数）；`is_admin` 校验。
- [ ] **T10 后端验收**：本地起 uvicorn，用 curl/脚本走通 T1-T9 全链路；用回归 4 用例跑提取 API，结果与基线 diff；**完成后打 tag `backend-v1`**。

### Phase 2：React 前端（`frontend/`）

- [ ] **T11 前端骨架**：Vite + TypeScript + Tailwind + shadcn；路由（工作台 / 管理后台 / 帮助 / 登录）；Supabase 风格全局样式 token（白底、直角、1px、#3ecf8e）；API 客户端封装（自动带会话令牌）。
- [ ] **T12 登录注册页**：注册（邀请码）/ 登录 / 记住会话；错误提示与加载态。
- [ ] **T13 工作台-上传与进度**：文件上传（拖拽 + 10MB 校验）、书名输入、参数设置（仅提取最大子目开关、Key 选择）、SSE 进度条、结果表格渲染。
- [ ] **T14 工作台-结果操作**：评分星标、AI 补充条目按钮、自由对话面板（SSE 打字机）、留言板、导出 Excel（沿用 openpyxl 生成、前端下载）。
- [ ] **T15 统计看板**：按类/流域柱状图、中国地图（复用 Plotly + assets/china_provinces.json 思路，前端用 ECharts 或 Plotly.js 均可）、高分条目列表。
- [ ] **T16 管理后台页**：用户管理、邀请码管理、全局 Key、提取记录管理（复用 T9 接口）。
- [ ] **T17 帮助页**：使用说明 + 版本日志（**纯用户语言，无技术细节**；v4.5.0「车书万里：一切从今始」起的历史沿革要保留）。
- [ ] **T18 前端联调验收**：本地 `pnpm dev` 连本地后端全流程走通；构建产物走通；**打 tag `frontend-v1`**。

### Phase 3：上线切换

- [ ] **T19 部署**：后端 → Render 免费档（Web Service，启动命令 `uvicorn app.main:app`，Build 装系统包 tesseract/poppler 的可选路径——若 RapidOCR 装不上则退 Tesseract，与现网一致）；前端 → Vercel；环境变量配齐（DEEPSEEK_API_KEY / SUPABASE_URL / SUPABASE_KEY / SESSION_SECRET / INVITE_CODE / ADMIN_USERNAMES）。
- [ ] **T20 保温与切换**：UptimeRobot 每 10 分钟 ping `/api/health`；全量回归过一遍生产地址；用户确认体验 → 入口切换（改 Vercel 域名或仓库描述指向）；Streamlit 版打 tag `streamlit-legacy` 后停更（**不删除**，可随时回滚）。

---

## 4. 给 Cline / 新会话的操作守则

1. 工作目录：仓库根目录（`C:\Users\杨睿\Desktop\pdf_extractor`，注意可能随机器变化，以实际为准）。
2. 环境：`.venv` 已建好（Python 3.14.7）；激活后装 `backend/requirements.txt` 依赖。
3. 回归：任何改动后运行 `python _run_regression.py`（需真实 DEEPSEEK_API_KEY，本地 .env 有）。
4. 提交规范：每条提交对应一个 ticket，message 格式 `T{n}: 简述`；完成后同步 push（网络不稳用重试循环）。
5. **禁令**：不提交 `.env`；不打印密钥；不改 `db_schema.sql` 已有表结构（如需变更先写迁移脚本）；不删 `示例/` 与回归基线。
6. 有疑问但无法询问用户时：**保持现有行为不变**（宁可保守），并把问题记入本文件的"待确认清单"。

### 待确认清单（用户决定，不阻塞开发）
- [ ] 前端部署用 Vercel 还是也放 Render（二选一，默认 Vercel）。
- [ ] 国内轻量服务器远期是否购买（不影响当前迁移）。
- [ ] GitHub 远程地址中的 PAT 需要轮换（安全项，提醒用户）。
- [ ] 设计稿：用户联系美术朋友出 Figma 稿 / Logo（可选，不阻塞）。

---

## 5. 版本沿革（帮助页文案素材，纯用户语言）

- **v4.7.0 车书万里**：提取更准、界面更顺（当前在线版）
- **v4.5.0 车书万里**：「一切从今始」——OCR 大升级，扫描版地方志也能稳稳提取
- 更早版本：召回率修复、过度提取修复、评分与留言板上线、统计看板上线

---

*本文件由 失败主义谋士千早爱音 维护；任何时候上下文满了，新会话先读本文件再动手。*
