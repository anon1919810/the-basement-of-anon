# 《卡尔特》v0.6 执行清单（交互手感 + 国界编辑器 + 省份重构 + 循环地图 + 视图）

> 状态：`[x]` = 已完成（提交 a1db119）

## 交互与视觉

- [x] **平滑缩放**：滚轮锚定光标平滑缩放（每帧插值/缓动，rAF 合并重绘；0.4x~4x；拖拽平移保留）
- [x] **左侧栏图标**：八分区各配 Lucide 图标（经济 Coins/市场 LineChart/税收 BadgePercent/阶级 Users2/人口 Users/政策 ScrollText/投资 Factory/大事记 Newspaper）

## 国界重绘系统（独立编辑模式）

- [x] 顶部「编辑模式」开关：进入暂停时钟 + 提示防误触；选中国家 → 点击省份改属 → 实时变色；迷雾区不可编辑
- [x] 撤销/重做（历史栈）、清空到默认；保存到 localStorage（`kalt-border-edits`）+ 导出 JSON 配置（`{provinceId: nationId}`，兼容 PROVINCE_OWNER_OVERRIDES，可下载文件）
- [x] 编辑模式与游戏模式隔离（快捷键禁用防误触；退出恢复时钟；开局自动加载 localStorage 覆盖）

## 省份重构（均匀化 + 海岛归并）

- [x] 大省拆分（>60 格按内部县 BFS 切分，目标 ~45 格）/ 小省并入（<10 格并入同属国最近邻）/ 相邻海岛（间距<35px 且同属国）归并为多岛省 → **55 省，规模 9-60 格**（盎格伦撒群岛 9 格为唯一边缘例外）
- [x] 确定性算法（同 seed 同结果）；资源（data/resources.json → 按新省 id 重算，写入 `data/resources_v2.json` + 前端内嵌生成，复用 山地出矿/沿海渔盐/低地农业/棉/林 规则，种子 1023）
- [x] 存档版本升级（v7）；8 国归属按新省份重派（v0.5 原则区域化，旧 id 覆盖表因重构失效，靠编辑器重建）

## 循环地图（东西环绕）

- [x] 渲染：世界宽度 W 处左右各复制一份无缝拼接，平移跨过 0/W 边界连续
- [x] 逻辑：省距/海峡距/运费/县距全部环绕（dx = min(|x1-x2|, W-|x1-x2|)）；经纬网格/地形底图/迷雾环绕
- [x] 缩放平移时环绕无缝；hover/点击取最近副本

## 视图切换

- [x] 地图右上角视图选择器：**政治 / 地形 / 人口 / 产值**（choropleth + 图例 + hover 数值随视图切换）
- [x] 人口=省份人口密度（绿→红）、产值=省份产出/工业值（蓝→橙）；政治=国家色、地形=底图

## 验证与交付

- [x] 8 国冒烟 + 洛林 50 年全断言（含环绕距离/新省份守恒/编辑器往返）；`npm run build` 0 错误，oxlint 0 错误
- [x] 提交推送 + 清单勾选
